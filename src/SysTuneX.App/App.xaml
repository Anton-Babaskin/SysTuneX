<Application x:Class="SysTuneX.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Theme is re-applied at start-up from the saved setting; Dark is only the seed. -->
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
                <ResourceDictionary Source="pack://application:,,,/Resources/Converters.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Resources/Theme.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Resources/PageParts.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!--
                Styles that derive from a WPF UI control style have to live here, not in
                Resources/Theme.xaml. A StaticResource inside a merged dictionary is resolved
                at parse time against that dictionary's own scope, and Theme.xaml does not
                merge ui:ControlsDictionary - so BasedOn would fail while the dictionary loads.
            -->
            <Style x:Key="FilterToggle" TargetType="ToggleButton" BasedOn="{StaticResource {x:Type ToggleButton}}">
                <Setter Property="Padding" Value="12,5" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Margin" Value="0,0,6,0" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
