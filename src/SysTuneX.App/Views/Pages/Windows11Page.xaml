<pages:SysTuneXPage x:Class="SysTuneX.App.Views.Pages.Windows11Page"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                    xmlns:loc="clr-namespace:SysTuneX.App.Localization"
                    xmlns:pages="clr-namespace:SysTuneX.App.Views.Pages"
                    xmlns:vm="clr-namespace:SysTuneX.App.ViewModels"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    mc:Ignorable="d"
                    d:DataContext="{d:DesignInstance Type=vm:Windows11ViewModel}"
                    Title="Windows 11">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="28,20,28,0"
                    MaxWidth="{StaticResource MaxContentWidth}"
                    HorizontalAlignment="Left">
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="{loc:Loc Windows11_Title}" Style="{StaticResource PageTitle}" />
                    <TextBlock Text="{loc:Loc Windows11_Subtitle}" Style="{StaticResource PageSubtitle}" />
                    <TextBlock Text="{Binding WindowsDescription}" Style="{StaticResource CaptionText}" Margin="0,4,0,0" />
                </StackPanel>

                <ContentControl Grid.Column="1" Template="{StaticResource TweakToolbarTemplate}" />
            </Grid>

            <!-- Windows 10 machines see most of this page filtered out, so say why. -->
            <ui:InfoBar Severity="Informational"
                        IsClosable="False"
                        Margin="0,0,0,12"
                        Title="{loc:Loc Windows11_Title}"
                        Message="{loc:Loc Banner_UnsupportedOs_Message}"
                        IsOpen="{Binding IsWindows11, Converter={StaticResource InverseBool}, Mode=OneWay}" />

            <ContentControl Template="{StaticResource ExplorerRestartBarTemplate}" />
            <ContentControl Template="{StaticResource TweakFilterBarTemplate}" />
        </StackPanel>

        <Grid Grid.Row="1">
            <ContentControl Template="{StaticResource TweakListTemplate}" />
            <ContentControl Template="{StaticResource BusyOverlayTemplate}" />
        </Grid>
    </Grid>
</pages:SysTuneXPage>
